# Public frontend configuration. This value is safe to expose in the browser.
VITE_API_BASE_URL=http://localhost:8000

# Current free-tier database outage notice. Set false and rebuild once restored.
# Defaults to true when omitted, so the notice is included in the next deployment.
VITE_DEMO_LIMIT_NOTICE=true
